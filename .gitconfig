[user]
    email = kapsh@kap.sh
    name = Alexander Kapshuna

[push]
    default = simple

[alias]

    # Shortcuts for simple commands
    co = checkout
    st = status -sb
    commend = commit --amend
    staged = diff --staged

    lg = log --graph --abbrev-commit --decorate --format=format:'\
%C(bold blue)%h%C(reset) \
%C(bold white)%s%C(reset)\
%C(magenta)%d%C(reset)\
%n        \
%C(green)%an%C(reset)\
%C(cyan) - %ar%C(reset) \
%C(yellow)(%ai)%C(reset)\
%n'

    # Rollback commit but keep changes
    undo = reset --mixed HEAD~1

    # Remove specified file from staged changes
    unstage = reset HEAD --

    # Update master and rebase on it
    shift = pull --rebase origin master

[core]
    excludesfile = .config/global_gitignore

[pager]
    branch = false
    stash = false

    # Is there a way to use variable for this?
    diff = diff-so-fancy | less --tabs=4 -R --pattern '^(Date|added|deleted|modified):'
    log = diff-so-fancy | less --tabs=4 -R --pattern '^(Date|added|deleted|modified):'
    show = diff-so-fancy | less --tabs=4 -R --pattern '^(Date|added|deleted|modified):'
    lg = less  # fancy and patterns interfere with graph

[color "diff-highlight"]
    oldNormal = red
    oldHighlight = red 52
    newNormal = green
    newHighlight = green 22

[color "diff"]
    meta = yellow
    frag = magenta bold
    commit = yellow bold
    old = red
    new = green
    whitespace = red reverse
